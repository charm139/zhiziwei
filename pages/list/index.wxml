<view class="container">
    <block wx:for="{{lists}}">
    <navigator url="../details/index?id={{item.id}}&term_id={{item.term_id}}&name={{item.post_title}}">
        <view class="brand_item">
              {{item.post_title}}
        </view>
        </navigator>
    </block>
</view>
