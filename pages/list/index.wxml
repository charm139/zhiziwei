<view class="container">
    <block wx:for="{{lists}}">
    <navigator url="../details/index?id={{item.id}}">
        <view class="brand_item">
              {{item.post_title}}
        </view>
        </navigator>
    </block>
</view>